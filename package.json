{
  "name": "osc-goes-brrr",
  "version": "1.0.0",
  "description": "Link between OSC and B.io",
  "main": "app/main.js",
  "scripts": {
    "start": "electron .",
    "build": "(cd src/main && npx webpack) && (cd src/frontend && npx webpack)",
    "postinstall": "(cd src && npm i) && (cd src/main && npm i) && (cd src/frontend && npm i)"
  },
  "repository": "https://gitlab.com/VRCFury/OscGoesBrrr",
  "license": "MIT",
  "devDependencies": {
    "electron": "^19.0.9",
    "electron-builder": "^23.1.0"
  },
  "build": {
    "npmRebuild": false,
    "icon": "app/tps-bio.png",
    "win": {
      "target": "zip"
    },
    "files": [
      "app/**/*"
    ]
  }
}
